import { prisma } from './src/lib/prisma'; async function main() { const pen = await prisma.pen.findFirst({ where: { OR: [{ code: 'HB16-17' }, { code: '16-17' }] } }); if (pen) { await prisma.pen.update({ where: { id: pen.id }, data: { code: 'HB17-17', name: 'Sàn nái h?u b? 17 - Ô 17' } }); console.log('Updated pen code to HB17-17'); } else { console.log('Pen not found'); } } main().finally(() => prisma.$disconnect());
